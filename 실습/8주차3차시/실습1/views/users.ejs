<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>댓글 전체 목록</h1>
    <h3>회원정보 추가</h3>
    <form action="insertUser" method="GET">
        <input type="text" placeholder="이름 입력" name="who" required /><br />
        <input type="text" placeholder="성별 입력" name="gender" required /><br />
        <input type="text" placeholder="전공 입력" name="major" required /><br />
        <button type="submit">제출</button>
    </form>
    <a href="/">홈으로 이동</a>
    <ul>
        <% for(let i = 0; i < lists.length; i++) { %>
            <li>
                <p>이름: <%= lists[i].who %></p>
                <p>성별: <%= lists[i].gender %></p>
                <p>전공: <%= lists[i].major %></p>
            </li>
            <p>----------------</p>
        <% } %>
    </ul>
</body>
</html>